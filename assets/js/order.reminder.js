/*
* 订单提醒相关
*/
layui.use(['element', 'form', 'laydate', 'common', 'inputMonitor', 'dispgrid', 'areadlg', 'productdlg'], function () {
    var $ = layui.jquery,
       layer = layui.layer,
       form = layui.form(),
       element = layui.element(),
       com = layui.common();

    com.init({
        callback: function () {

        }
    });

});